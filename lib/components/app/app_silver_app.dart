import 'package:flutter/material.dart';

class AppSilverApp extends StatelessWidget {
  const AppSilverApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}